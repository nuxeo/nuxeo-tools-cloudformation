This template deploys an elasticsearch cluster inside a VPC.
The VPC must already exist and have a private subnet (it is not created by the template).

The base AMIs are Debian Jessie 8.1 ones - the SSH user is "admin".


